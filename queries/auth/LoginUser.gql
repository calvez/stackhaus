# Query settings
mutation LoginUser($password: String, $clientMutationId: String) {
    login(
        input: {
            clientMutationId: clientMutationId
            username: "funkhaus"
            password: password
        }
    ) {
        authToken
        user {
            id
            name
        }
    }
}
